name: Sync from Organization Repo

on:
  workflow_dispatch:        # Permite rodar manualmente
  schedule:
    - cron: "0 */6 * * *"   # Executa a cada 6 horas (ajuste se quiser)

name: Sync from Organization Repo

# ... (on: e jobs: deploy: runs-on: está correto)

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout target repo (this one)
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Configure git
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "actions@github.com"

      - name: Clone source (organization private repo)
        env:
          ORG_ACCESS_TOKEN: ${{ secrets.TOKEN_REPO }} 
        run: |
          # ❌ Removido: rm -rf source-repo (Será substituído pelo git clone)
          
          # Clona o repositório privado da organização
          git clone --depth=1 https://x-access-token:${ORG_ACCESS_TOKEN}@github.com/FieldFlowJJ/FIELD-FLOW-FRONTED.git source-repo
          cd source-repo
          git checkout main
          cd ..

      - name: Sync files
        run: |
          # 1. Limpa o diretório de trabalho local (EXCETO a pasta .git)
          #    Isso garante que o rsync não tenha que deletar arquivos no source-repo.
          find . -maxdepth 1 ! -name .git ! -name . -exec rm -rf {} \;
          
          # 2. Copia o conteúdo da pasta clonada para a raiz
          #    --exclude='.git' é mantido para não copiar o repositório git da origem.
          rsync -av --delete --exclude='.git' source-repo/ ./

      - name: Commit and push changes
        run: |
          git add .
          if git diff --cached --quiet; then
            echo "Nenhuma alteração detectada. Nada para enviar."
            exit 0
          fi
          git commit -m "🔄 Sync from organization repo [auto]"
          git push origin main